<div class="row m-3">
	<div class="col-4">
		<div class="card" style="width: 18rem;">
			<div class="card-body">
				<h5 class="card-title">{{project.nameproject}}</h5>
				<p class="card-text">{{project.link}}</p>
				<a href="{{ path('app_project_edit', {'id': project.id}) }}" class="btn btn-primary">Go somewhere</a>
			</div>
		</div>
	</div>
</div>
